
;毛並みシャッフル
@NAMING_PET_SHUFFLE_COAT

LAST_SHUFFLE_S = %PET_COAT%
STR_MAX = 49
CALL STR_REF_SHUFFLE(STR_REF +100)
IF SHUFFLE_COUNT < 10 && LAST_SHUFFLE_S == RESULTS
	SHUFFLE_COUNT ++
	RESTART
ENDIF
SHUFFLE_COUNT = 0
PET_COAT = %RESULTS%



;毛並み候補表示
@NAMING_PET_SELECT_COAT
#DIM LOOP_REF

LOOP_REF = 100
PRINTL 选择毛发。
BR = 0
FOR LOCAL,STR_REF +LOOP_REF,STR_REF +LOOP_REF +50
	SIF STR:LOCAL == ""
		CONTINUE
	CALL BR_PLUS(3)
	PRINTFORMLC [{LOCAL-STR_REF -LOOP_REF,3}] %STR:LOCAL%
NEXT
PRINTL
PRINTL [900] 取消
INPUT
SIF RESULT >= 0 && RESULT < 50 && STR:(STR_REF +LOOP_REF +RESULT) != ""
	PET_COAT = %STR:(STR_REF +LOOP_REF +RESULT)%



;模様シャッフル
@NAMING_PET_SHUFFLE_PATTERN

LAST_SHUFFLE_S = %PET_PATTERN%
STR_MAX = 49
CALL STR_REF_SHUFFLE(STR_REF +150)

;1/4で空欄にする
SIF !RAND:4
	RESULTS =
IF SHUFFLE_COUNT < 10 && LAST_SHUFFLE_S == RESULTS
	SHUFFLE_COUNT ++
	RESTART
ENDIF
SHUFFLE_COUNT = 0
PET_PATTERN = %RESULTS%



;模様候補表示
@NAMING_PET_SELECT_PATTERN
#DIM LOOP_REF

LOOP_REF = 150
PRINTL 选择花纹。
BR = 0
FOR LOCAL,STR_REF +LOOP_REF,STR_REF +LOOP_REF +50
	SIF STR:LOCAL == ""
		CONTINUE
	CALL BR_PLUS(3)
	PRINTFORMLC [{LOCAL-STR_REF -LOOP_REF,3}] %STR:LOCAL%
NEXT
PRINTL
PRINTL [100] （无花纹）
PRINTL [900] 取消
INPUT
SIF RESULT >= 0 && RESULT < 50 && STR:(STR_REF +LOOP_REF +RESULT) != ""
	PET_PATTERN = %STR:(STR_REF +LOOP_REF +RESULT)%
SIF RESULT == 100
	PET_PATTERN =



;いろいろ手動入力
;ARGS→入力項目
@NAMING_PET_MANUAL(ARGS)

SELECTCASE ARGS
CASE "名称"
	PRINTL 请输入名称。
	INPUTS
	IF RESULTS != ""
		PET_NAME = %RESULTS%
		CALL NAMING_PET_NAME_FIX
	ENDIF
	
CASE "性格"
	PRINTL 请输入毛发。
	INPUTS
	SIF RESULTS != ""
		PET_COAT = %RESULTS%
	
CASE "花纹"
	PRINTL 请输入花纹。
	INPUTS
	PET_PATTERN = %RESULTS%
ENDSELECT






