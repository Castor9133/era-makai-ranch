
;名前が重複していないかチェック
@NAME_DUPLICATE_CHECK

;CHARANUM-1にすれば現在のキャラとの比較になる
FOR TARGET_COUNT,0,CHARANUM -2
	IF CALLNAME == CALLNAME:TARGET_COUNT
		SETCOLOR 0xFFFF00
		PRINT ！重复的名称
		RESETCOLOR
		BREAK
	ENDIF
NEXT


