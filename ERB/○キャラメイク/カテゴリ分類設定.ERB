
;カテゴリ分類設定
;固有のヘアスタイルにしたり作品固有の種族名を使ってもドット絵表示とかできるようにする処理だよ
;シャッフル後に逐一設定に行くようにしたので不要と言えばそうだけど利便性のため残すのがよさげ
;ARG→上書きしない（CSV読み込みとか）
@CHOICE_CATEGORY(ARG)

;野郎はどうでもいい
SIF TALENT:男性
	RETURN
CALL CATEGORY_SKIN_COLOR(ARG)
CALL CATEGORY_EYE_COLOR(ARG)
CALL CATEGORY_HAIR_COLOR(ARG)
CALL CATEGORY_HAIR_FEATURE(ARG)
CALL CATEGORY_HAIR_STYLE(ARG)
CALL CATEGORY_TRIVE(ARG)
CALL CATEGORY_PERSONALITY(ARG)
EXIST = 0



;肌の色
@CATEGORY_SKIN_COLOR(ARG)

STR_REF = 100
FOR LOCAL,STR_REF,STR_REF +50
	SIF STR:LOCAL == ""
		CONTINUE
	;カテゴリを指定されてる場合に対応する　ARG=1で上書きしない
	SIF STR:LOCAL == CSTR:颜色类别／肤色 && ARG
		RETURN
	IF STR:LOCAL == CSTR:肤色
		CSTR:颜色类别／肤色 = %CSTR:肤色%
		RETURN
	ENDIF
NEXT
PRINTFORML %CALLNAME%的肤色「%CSTR:肤色%」不存在于类别中。请选择最近的类别。
PRINTFORML （无论选择的内容如何，状态上的显示都会以「%CSTR:肤色%」显示）
FOR LOCAL,STR_REF,STR_REF +50
	SIF STR:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL -STR_REF,3}] %STR:LOCAL%
NEXT
BINPUT
CSTR:颜色类别／肤色 = %STR:(RESULT +STR_REF)%



;左右の目入り口
@CATEGORY_EYE_COLOR(ARG)

CALL CATEGORY_RIGHT_EYE_COLOR(ARG)
CALL CATEGORY_LEFT_EYE_COLOR(ARG)



;右目
@CATEGORY_RIGHT_EYE_COLOR(ARG)

STR_REF = 150
FOR LOCAL,STR_REF,STR_REF +50
	SIF STR:LOCAL == ""
		CONTINUE
	SIF STR:LOCAL == CSTR:颜色类别／右瞳色 && ARG
		RETURN
	IF STR:LOCAL == CSTR:右瞳色
		CSTR:颜色类别／右瞳色 = %CSTR:右瞳色%
		RETURN
	ENDIF
NEXT
PRINTFORML %CALLNAME%的右瞳色「%CSTR:右瞳色%」不存在于类别中。请选择最近的类别。
PRINTFORML （无论选择的内容如何，状态上的显示都会以「%CSTR:右瞳色%」显示）
FOR LOCAL,STR_REF,STR_REF +50
	SIF STR:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL -STR_REF,3}] %STR:LOCAL%
NEXT
BINPUT
CSTR:颜色类别／右瞳色 = %STR:(RESULT +STR_REF)%



;左目
@CATEGORY_LEFT_EYE_COLOR(ARG)

;左目の色は空欄になることもあるので空欄だと帰る
IF CSTR:左瞳色 == ""
	CSTR:颜色类别／左瞳色 =
	RETURN
ENDIF
STR_REF = 150
FOR LOCAL,STR_REF,STR_REF +50
	SIF STR:LOCAL == ""
		CONTINUE
	SIF STR:LOCAL == CSTR:颜色类别／左瞳色 && ARG
		RETURN
	IF STR:LOCAL == CSTR:左瞳色
		CSTR:颜色类别／左瞳色 = %CSTR:左瞳色%
		RETURN
	ENDIF
NEXT
PRINTFORML %CALLNAME%的左瞳色「%CSTR:左瞳色%」不存在于类别中。请选择最近的类别。
PRINTFORML （无论选择的内容如何，状态上的显示都会以「%CSTR:左瞳色%」显示）
FOR LOCAL,STR_REF,STR_REF +50
	SIF STR:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL -STR_REF,3}] %STR:LOCAL%
NEXT
BINPUT
CSTR:颜色类别／左瞳色 = %STR:(RESULT +STR_REF)%



;髪色
@CATEGORY_HAIR_COLOR(ARG)

STR_REF = 200
FOR LOCAL,STR_REF,STR_REF +100
	SIF STR:LOCAL == ""
		CONTINUE
	SIF STR:LOCAL == CSTR:颜色类别／发色 && ARG
		RETURN
	IF STR:LOCAL == CSTR:发色
		CSTR:颜色类别／发色 = %CSTR:发色%
		RETURN
	ENDIF
NEXT
PRINTFORML %CALLNAME%的发色「%CSTR:发色%」不存在于类别中。请选择最近的类别。
PRINTFORML （无论选择的内容如何，状态上的显示都会以「%CSTR:发色%」显示）
FOR LOCAL,STR_REF,STR_REF +100
	SIF STR:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL -STR_REF,3}] %STR:LOCAL%
NEXT
BINPUT
CSTR:颜色类别／发色 = %STR:(RESULT +STR_REF)%



;髪特徴
@CATEGORY_HAIR_FEATURE(ARG)

;髪特徴も空欄がありえる
IF CSTR:头发特征 == ""
	CSTR:颜色类别／发特征 =
	RETURN
ENDIF
STR_REF = 300
FOR LOCAL,STR_REF,STR_REF +100
	SIF STR:LOCAL == ""
		CONTINUE
	SIF STR:LOCAL == CSTR:颜色类别／发特征 && ARG
		RETURN
	IF STR:LOCAL == CSTR:头发特征
		CSTR:颜色类别／发特征 = %CSTR:头发特征%
		RETURN
	ENDIF
NEXT
PRINTFORML %CALLNAME%的头发特征「%CSTR:头发特征%」不存在于类别中。请选择最近的类别。
PRINTFORML （无论选择的内容如何，状态上的显示都会以「%CSTR:头发特征%」显示）
FOR LOCAL,STR_REF,STR_REF +100
	SIF STR:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL -STR_REF,3}] %STR:LOCAL%
NEXT
BINPUT
CSTR:颜色类别／发特征 = %STR:(RESULT +STR_REF)%



;髪型
@CATEGORY_HAIR_STYLE(ARG)

STR_REF = 500
FOR LOCAL,STR_REF,STR_REF +100
	SIF STR:LOCAL == ""
		CONTINUE
	SIF STR:LOCAL == CSTR:颜色类别／发型 && ARG
		RETURN
	IF STR:LOCAL == CSTR:发型
		CSTR:颜色类别／发型 = %CSTR:发型%
		RETURN
	ENDIF
NEXT
PRINTFORML %CALLNAME%的发型「%CSTR:发型%」不存在于类别中。请选择最近的类别。
PRINTFORML （无论选择的内容如何，状态上的显示都会以「%CSTR:发型%」显示）
FOR LOCAL,STR_REF,STR_REF +100
	SIF STR:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL -STR_REF,3}] %STR:LOCAL%
NEXT
BINPUT
CSTR:颜色类别／发型 = %STR:(RESULT +STR_REF)%



;種族
@CATEGORY_TRIVE(ARG)

STR_REF = 1000
FOR LOCAL,STR_REF,STR_REF +300
	SIF STR:LOCAL == ""
		CONTINUE
	SIF STR:LOCAL == CSTR:颜色类别／种族 && ARG
		RETURN
	IF STR:LOCAL == CSTR:种族
		CSTR:颜色类别／种族 = %CSTR:种族%
		RETURN
	ENDIF
NEXT
PRINTFORML %CALLNAME%的种族「%CSTR:种族%」不存在于类别中。请选择最近的类别。
PRINTFORML （无论选择的内容如何，状态上的显示都会以「%CSTR:种族%」显示）
FOR LOCAL,STR_REF,STR_REF +300
	SIF STR:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL -STR_REF,3}] %STR:LOCAL%
NEXT
BINPUT
CSTR:颜色类别／种族 = %STR:(RESULT +STR_REF)%



;性格
@CATEGORY_PERSONALITY(ARG)

STR_REF = 3000
FOR LOCAL,STR_REF,STR_REF +100
	SIF STR:LOCAL == ""
		CONTINUE
	SIF STR:LOCAL == CSTR:颜色类别／性格 && ARG
		RETURN
	IF STR:LOCAL == CSTR:性格
		CSTR:颜色类别／性格 = %CSTR:性格%
		RETURN
	ENDIF
NEXT
PRINTFORML %CALLNAME%的性格「%CSTR:性格%」不存在于类别中。请选择最近的类别。
PRINTFORML （无论选择的内容如何，状态上的显示都会以「%CSTR:性格%」显示）
FOR LOCAL,STR_REF,STR_REF +100
	SIF STR:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL -STR_REF,3}] %STR:LOCAL%
NEXT
BINPUT
CSTR:颜色类别／性格 = %STR:(RESULT +STR_REF)%


