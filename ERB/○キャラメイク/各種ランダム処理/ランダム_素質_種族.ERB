
;奴隷候補の素質_種族
@RANDOM_SLAVE_TALENT_TRIBE

;種族での素質判定を作ったのでそれ準拠になったよ
FLAG_NAME = 种族设定／%CSTR:种族%
TRIBE_FLAG_A = 外观反映率／%CSTR:种族%
TRIBE_FLAG_C = 体质反映率／%CSTR:种族%
TRIBE_FLAG_P = 性格反映率／%CSTR:种族%
SELECTCASE CSTR:种族
CASE "精灵","暗精灵","半精灵"
	TALENT:精灵耳 = 1

CASE "半身人"
	TALENT:半身人耳 = 1

CASE "犬耳族"
	TALENT:动物耳朵 = 1
	TALENT:尾巴 = 1
	SIF GETBIT(FLAG:FLAG_NAME,0) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:锯齿 = 1
	IF GETBIT(FLAG:FLAG_NAME,1) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:好奇心 = 1
		TALENT:保守的 = 0
	ENDIF
	IF GETBIT(FLAG:FLAG_NAME,2) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:污臭敏感 = 1
		TALENT:污臭钝感 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,3) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:隶属愿望 = 1

CASE "猫耳族"
	TALENT:动物耳朵 = 1
	TALENT:尾巴 = 1
	SIF GETBIT(FLAG:FLAG_NAME,0) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:猫舌 = 1
	SIF GETBIT(FLAG:FLAG_NAME,1) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:喜欢泡澡 = -1
	SIF GETBIT(FLAG:FLAG_NAME,2) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:喜怒无常 = 1
		
CASE "兔耳族"
	TALENT:动物耳朵 = 1
	TALENT:尾巴 = 1
	SIF GETBIT(FLAG:FLAG_NAME,0) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:胆怯 = 1
	SIF GETBIT(FLAG:FLAG_NAME,1) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:喜欢偷懒 = 1
		
CASE "狐耳族"
	TALENT:动物耳朵 = 1
	TALENT:尾巴 = 1
	SIF GETBIT(FLAG:FLAG_NAME,0) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:煽动 = 1
	IF GETBIT(FLAG:FLAG_NAME,1) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		;高慢が入ってたらそのままにして排他させる
		TALENT:自尊心高 = 1
		TALENT:自尊心低 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,2) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:喜欢偷懒 = 1
	
CASE "狸耳族"
	TALENT:动物耳朵 = 1
	TALENT:尾巴 = 1
	IF GETBIT(FLAG:FLAG_NAME,0) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:乐观 = 1
		TALENT:悲观 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,1) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:喜欢泡澡 = 1
		
CASE "牛角族"
	TALENT:动物耳朵 = 1
	TALENT:尾巴 = 1
	TALENT:两角 = 1
	IF GETBIT(FLAG:FLAG_NAME,0) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:贪吃 = 1
		TALENT:少食 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,1) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:老实 = 1
		
CASE "羊角族"
	TALENT:动物耳朵 = 1
	TALENT:尾巴 = 1
	TALENT:两角 = 1
	SIF GETBIT(FLAG:FLAG_NAME,0) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:老实 = 1
		
CASE "仙人"
	IF GETBIT(FLAG:FLAG_NAME,3) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:回复快 = 1
		TALENT:回复慢 = 0
	ENDIF
	IF GETBIT(FLAG:FLAG_NAME,4) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:受动物喜欢 = 1
		TALENT:被动物讨厌 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,5) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:魔界适应 = 1
	
	IF GETBIT(FLAG:FLAG_NAME,10) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:贪吃 = 0
		TALENT:少食 = 1
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,11) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:坚强 = 1
	SIF GETBIT(FLAG:FLAG_NAME,12) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:自制力 = 1
	SIF GETBIT(FLAG:FLAG_NAME,13) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:正义感 = 1
		
CASE "龙人"
	TALENT:尾巴 = 1
	TALENT:两角 = 1
	IF GETBIT(FLAG:FLAG_NAME,1) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:不怕痛 = 1
		TALENT:怕痛 = 0
	ENDIF
	IF GETBIT(FLAG:FLAG_NAME,2) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:回复快 = 1
		TALENT:回复慢 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,3) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:锯齿 = 1
	SIF GETBIT(FLAG:FLAG_NAME,4) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:药毒耐性 = 1
	
	IF GETBIT(FLAG:FLAG_NAME,10) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:贪吃 = 1
		TALENT:少食 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,11) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:抗拒 = 1
	IF GETBIT(FLAG:FLAG_NAME,12) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:自尊心高 = 1
		TALENT:自尊心低 = 0
	ENDIF
	IF GETBIT(FLAG:FLAG_NAME,13) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:粗野 = 1
		TALENT:清纯 = 0
		TALENT:高雅 = 0
	ENDIF
		;SIF GETBIT(FLAG:FLAG_NAME,14) && (RAND:100 < FLAG:TRIBE_FLAG_P)
			;TALENT:性知識乏しい = 1
	SIF GETBIT(FLAG:FLAG_NAME,15) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:爱慕虚荣 = 1
		
CASE "妖狐"
	TALENT:动物耳朵 = 1
	TALENT:尾巴 = 1
	TALENT:魔界适应 = 1
	IF GETBIT(FLAG:FLAG_NAME,5) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:回复快 = 1
		TALENT:回复慢 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,6) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:药毒耐性 = 1
	IF GETBIT(FLAG:FLAG_NAME,7) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:受动物喜欢 = 0
		TALENT:被动物讨厌 = 1
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,8) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:怕太阳 = 1
	
	SIF GETBIT(FLAG:FLAG_NAME,10) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:煽动 = 1
	SIF GETBIT(FLAG:FLAG_NAME,11) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:傲慢 = 1
	IF GETBIT(FLAG:FLAG_NAME,12) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:自尊心高 = 1
		TALENT:自尊心低 = 0
	ENDIF
	
	;高慢は排他でプライド系の上位になるのでこれでOK
	SIF GETBIT(FLAG:FLAG_NAME,13) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:高傲 = 1
	SIF GETBIT(FLAG:FLAG_NAME,14) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:献身的 = 1
	SIF GETBIT(FLAG:FLAG_NAME,15) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:抖Ｓ = 1
	SIF GETBIT(FLAG:FLAG_NAME,16) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:喜欢泡澡 = -1
	SIF GETBIT(FLAG:FLAG_NAME,17) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:爱慕虚荣 = 1
		
CASE "吸血鬼"
	TALENT:魔界适应 = 1
	IF GETBIT(FLAG:FLAG_NAME,3) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:回复快 = 1
		TALENT:回复慢 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,4) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:药毒耐性 = 1
	IF GETBIT(FLAG:FLAG_NAME,5) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:受动物喜欢 = 0
		TALENT:被动物讨厌 = 1
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,6) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:怕太阳 = 1
	
	IF GETBIT(FLAG:FLAG_NAME,10) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:不怕痛 = 1
		TALENT:怕痛 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,11) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:煽动 = 1
	IF GETBIT(FLAG:FLAG_NAME,12) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:贪吃 = 0
		TALENT:少食 = 1
	ENDIF
	IF GETBIT(FLAG:FLAG_NAME,13) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:自尊心高 = 1
		TALENT:自尊心低 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,14) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:高傲 = 1
	SIF GETBIT(FLAG:FLAG_NAME,15) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:领袖魅力 = 1
	SIF GETBIT(FLAG:FLAG_NAME,16) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:抖Ｓ = 1
	IF GETBIT(FLAG:FLAG_NAME,17) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:高雅 = 1
		TALENT:粗野 = 0
	ENDIF
		
CASE "半吸血鬼"
	IF GETBIT(FLAG:FLAG_NAME,2) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:回复快 = 1
		TALENT:回复慢 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,3) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:药毒耐性 = 1
	IF GETBIT(FLAG:FLAG_NAME,4) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:受动物喜欢 = 0
		TALENT:被动物讨厌 = 1
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,5) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:怕太阳 = 1
	SIF GETBIT(FLAG:FLAG_NAME,7) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:魔界适应 = 1
	
	SIF GETBIT(FLAG:FLAG_NAME,10) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:煽动 = 1
	IF GETBIT(FLAG:FLAG_NAME,11) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:贪吃 = 0
		TALENT:少食 = 1
	ENDIF
	IF GETBIT(FLAG:FLAG_NAME,12) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:自尊心高 = 1
		TALENT:自尊心低 = 0
	ENDIF
	
	SIF GETBIT(FLAG:FLAG_NAME,13) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:高傲 = 1
	SIF GETBIT(FLAG:FLAG_NAME,14) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:领袖魅力 = 1
	SIF GETBIT(FLAG:FLAG_NAME,15) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:抖Ｓ = 1
	IF GETBIT(FLAG:FLAG_NAME,16) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:高雅 = 1
		TALENT:粗野 = 0
	ENDIF

CASE "天使"
	TALENT:天界种族 = 1
	IF GETBIT(FLAG:FLAG_NAME,3) && TALENT:处女 && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:贞操观念 = 1
		TALENT:不在乎贞操 = 0
	ENDIF
	IF GETBIT(FLAG:FLAG_NAME,4) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:受动物喜欢 = 1
		TALENT:被动物讨厌 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,5) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:药毒耐性 = 1
		
	SIF GETBIT(FLAG:FLAG_NAME,10) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:坦率 = 1
	IF GETBIT(FLAG:FLAG_NAME,11) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:不怕痛 = 1
		TALENT:怕痛 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,12) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:自制力 = 1
	SIF GETBIT(FLAG:FLAG_NAME,13) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:难以越过的底线 = 1
	IF GETBIT(FLAG:FLAG_NAME,14) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:害羞 = 1
		TALENT:不知羞恥 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,15) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:献身的 = 1
	IF GETBIT(FLAG:FLAG_NAME,16) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:接受快感 = 0
		TALENT:否定快感 = 1
	ENDIF
	IF GETBIT(FLAG:FLAG_NAME,17) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:清纯 = 1
		TALENT:粗野 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,18) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:正义感 = 1
		
CASE "天人"
	TALENT:天界种族 = 1
	IF GETBIT(FLAG:FLAG_NAME,3) && TALENT:处女 && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:贞操观念 = 1
		TALENT:不在乎贞操 = 0
	ENDIF
	IF GETBIT(FLAG:FLAG_NAME,4) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:受动物喜欢 = 1
		TALENT:被动物讨厌 = 0
	ENDIF
	IF GETBIT(FLAG:FLAG_NAME,5) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:回复快 = 1
		TALENT:回复慢 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,6) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:魔界适应 = 1
	SIF GETBIT(FLAG:FLAG_NAME,7) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:药毒耐性 = 1
		
	SIF GETBIT(FLAG:FLAG_NAME,10) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:高雅 = 1
	IF GETBIT(FLAG:FLAG_NAME,11) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:不怕痛 = 1
		TALENT:怕痛 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,12) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:自制力 = 1
	SIF GETBIT(FLAG:FLAG_NAME,13) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:难以越过的底线 = 1
	IF GETBIT(FLAG:FLAG_NAME,14) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:自尊心高 = 1
		TALENT:自尊心低 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,15) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:高傲 = 1
	IF GETBIT(FLAG:FLAG_NAME,16) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:接受快感 = 0
		TALENT:否定快感 = 1
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,17) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:正义感 = 1
		
CASE "女神"
	TALENT:天界种族 = 1
	IF GETBIT(FLAG:FLAG_NAME,3) && TALENT:处女 && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:贞操观念 = 1
		TALENT:不在乎贞操 = 0
	ENDIF
	IF GETBIT(FLAG:FLAG_NAME,4) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:受动物喜欢 = 1
		TALENT:被动物讨厌 = 0
	ENDIF
	IF GETBIT(FLAG:FLAG_NAME,5) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:回复快 = 1
		TALENT:回复慢 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,6) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:魔界适应 = 1
	SIF GETBIT(FLAG:FLAG_NAME,7) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:药毒耐性 = 1
		
	SIF GETBIT(FLAG:FLAG_NAME,10) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:高雅 = 1
	IF GETBIT(FLAG:FLAG_NAME,11) && (RAND:100 < FLAG:TRIBE_FLAG_C)
		TALENT:不怕痛 = 1
		TALENT:怕痛 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,12) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:自制力 = 1
	SIF GETBIT(FLAG:FLAG_NAME,13) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:难以越过的底线 = 1
	IF GETBIT(FLAG:FLAG_NAME,14) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:自尊心高 = 1
		TALENT:自尊心低 = 0
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,15) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:高傲 = 1
	IF GETBIT(FLAG:FLAG_NAME,16) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:接受快感 = 0
		TALENT:否定快感 = 1
	ENDIF
	SIF GETBIT(FLAG:FLAG_NAME,17) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:正义感 = 1
	SIF GETBIT(FLAG:FLAG_NAME,18) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:献身的 = 1
	SIF GETBIT(FLAG:FLAG_NAME,19) && (RAND:100 < FLAG:TRIBE_FLAG_P)
		TALENT:领袖魅力 = 1
		
ENDSELECT



