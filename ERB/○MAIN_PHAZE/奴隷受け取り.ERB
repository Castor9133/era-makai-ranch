
;牧場で奴隷を受け取る処理
@SLAVE_RECEIVE_RANCH

CALL CHASE_SLAVE_CHECK
SIF !FLAG:追踪中人数
	RETURN

FOR TARGET_COUNT,1,CHARANUM
	TARGET = TARGET_COUNT
	
	;追跡対象3＝輸送中の場合のみ実行
	SIF CFLAG:追踪对象 != 3
		CONTINUE
	PRINTFORML 捕获的%CALLNAME%已到达%SAVESTR:牧场名%。
	CALL SLAVE_RECEIVE_COMMON
NEXT
TARGET = -1


