# 口上创作 DEMO 与性格扩展清单（era-makai-ranch）

本文档供你**自写多套泛用口上**时使用：前半是**工程内接法**与**最小可跑 DEMO**；中段是**与常见二次元游戏相比容易缺的 archetype**；末段是**推荐 8～10 个新「性格」词条**（写入 `Str.csv` 3000 段 + 随机性格池 + 可选口上套装名）。

**与系统同步的硬性约定**（新道具 / 指令 / 体位等改代码时口上必须跟）：见 **`docs/口上与玩法扩展同步规则.md`**。AI 或协作者若写不好口上，应列出欠番清单交你补写。

---

## 一、本工程里「性格」和「口上」的关系（必读）

| 概念 | 存哪 | 作用 |
|------|------|------|
| **性格** | `CSTR:性格`（文字，如「强势」） | 随机素質、称呼等**数值/逻辑**会参考；**不自动绑定**口上。 |
| **使用口上** | `CSTR:使用口上`（如「強気1」） | `KOJO_PRINT` → `TRYCALLFORM KOJO_GENERAL_INDEX_%CSTR:使用口上%`。 |
| **口上显示** | `CFLAG:口上显示`、`FLAG:口上显示` | 关则整局不显示。 |

**泛用口上**要同时做三件事：

1. 新建 `ERB/○口上/汎用口上/〇〇口上1.ERB`（或你起的套装名），内含 **`@KOJO_GENERAL_INDEX_口上識別名(ARGS:0,ARGS:1,ARGS:2,ARGS:3)`**。  
   - 函数名**必须**与 `CSTR:使用口上` 完全一致（例：`KOJO_GENERAL_INDEX_懦弱1`）。  
2. 在 **`CSV/Str.csv` 的 6100～** 增加一行：**编号,口上識別名**（与上面函数后缀一致）。  
3. 在游戏内「口上设置」里能枚举到该函数（`ENUMFUNCWITH "KOJO_GENERAL_INDEX_"`）。

台词用 **`CALL MESSAGE_BOX,@"……"`** 放入候选箱，由 **`MESSAGE_PRINT`** 随机抽一条显示。  
换行用 **`\n`**（见 `○口上COMMON.ERB` 注释）。

**汉化向约定**：玩家在游戏里看到的口上正文，建议**统一写简体中文**；`CASE "……"` 里的指令名、`%MASTER_CALL()%` 等占位符仍须与工程内日文标识一致，勿改。

---

## 二、`KOJO_PRINT` 常见第一参数 `ARGS:0`（写作目录）

下列来自仓库内 `CALL KOJO_PRINT(...)` 的整理，**新口上至少建议先覆盖加粗**的高频项；其余可 `CASEELSE` 留空或接共通默认。

| ARGS:0 | 说明（第二、第三参数常见形态） |
|--------|----------------------------------|
| **调教命令** | `ARGS:1` = 指令名（`TRAINNAME:SELECTCOM`）、`ARGS:2` = `"执行"` / `"移除"` 等 |
| **射精** | 常与 `SOURCE:膣内射精经验` 等配合在口内分支写 |
| **绝顶** | 高潮时 |
| **日程** | 如 `"调教","开始调教"` / `"调教结束"` |
| **事件** | 如领取奴隶、母乳体质化等 |
| **战斗** | 攻击、被击中、战斗开始等 |
| **访问** | 入口、改称呼、改发型等 |
| **吸欧派** | 开始 / 结束 |
| **例文** | 口上设置界面展示用，**建议必写**一句自我介绍 |

更多分支可在现有 **`汎用口上/*口上1.ERB`** 里搜 `SELECTCASE ARGS:0` / `GROUPMATCH(ARGS:1` 照抄结构。

---

## 二点五、单一文件整套范本（推荐直接复制改）

仓库内已提供 **完整一套「懦弱」泛用口上**，全部写在一个 `.ERB` 里（含 `TRAIN`），作为生产其他性格的母版：

- **`ERB/○口上/汎用口上/懦弱口上1.ERB`**
- **`Str.csv`** 已登记 **`6110,懦弱1`**

游戏内把奴隶 **`CSTR:使用口上`** 设为 **`懦弱1`** 并打开口上显示即可测试。复制后全局替换 **`懦弱1` / `懦弱口上1` / `KOJO_GENERAL_INDEX_懦弱1` / `KOJO_GENERAL_懦弱1_TRAIN`** 等标识即可套壳写新性格。

---

## 三、最小可跑 DEMO（复制后改名字）

**文件**：`ERB/○口上/汎用口上/DEMO性格口上1.ERB`（文件名随意，但**索引名**要与 `Str.csv` 一致）

```eramaker
; ======================================
;口上識別名：DEMO性格1
;口上作者：（你的名字）
;许可证：CC BY-NC（与现有泛用口上统一即可，或自填）
; ======================================

@KOJO_GENERAL_INDEX_DEMO性格1(ARGS:0,ARGS:1,ARGS:2,ARGS:3)

SELECTCASE ARGS:0
CASE "例文"
	CALL MESSAGE_BOX,@"「这里是【DEMO性格1】口上样例。\n陷落前后、命令与绝顶会按性格分叉。」"

CASE "调教命令"
	; 最简：不区分指令，先写通用品
	IF ARGS:2 == "执行"
		CALL MESSAGE_BOX,@"「……（执行 %ARGS:1% 时的台词）」"
	ELSEIF ARGS:2 == "移除"
		CALL MESSAGE_BOX,@"「……（移除装备时的台词）」"
	ENDIF

CASE "绝顶"
	CALL MESSAGE_BOX,@"「……要去了……」"

CASE "射精"
	IF SOURCE:膣内射精经验
		CALL MESSAGE_BOX,@"「……里面……」"
	ELSE
		CALL MESSAGE_BOX,@"「……射在外面……」"
	ENDIF

CASEELSE
	; 未实现的分支留空即可
ENDSELECT
RETURN
```

**`Str.csv`（6100 段附近）追加一行**（编号不与现有重复即可）：

```csv
6110,DEMO性格1
```

游戏内把该奴隶 **`CSTR:使用口上`** 设为 **`DEMO性格1`**，打开口上显示，执行任意带 `KOJO_PRINT("调教命令",...)` 的指令即可测。

**口枷 / 深喉等**：多数泛用口上在 `TFLAG:Ｍ装备` 时 `CALL KOJO_GENERAL_MOUTH_SHACKLE_1` 直接 `RETURN`；你可在 DEMO 后期再接同一套路，避免开局工作量爆炸。

---

## 四、和「一般手游 / 拔作」比，现有泛用口上**容易缺**的维度

当前仓库泛用套装大致覆盖：**强气、元气、品、姐系、认真、堅物、无口、幼め非敬语、男孩子气、慇懃**。  
相对**常见卡池**仍容易缺（你举的例子落在这些里）：

| 缺口类型 | 玩家直觉标签 | 备注 |
|----------|--------------|------|
| **低反抗 / 高顺从** | 懦弱、胆小、受け身 | 与「弱势」不同：弱势可偏傲；**懦弱**更偏「不敢拒绝、道歉多」。 |
| **和风母性 / 外柔内刚** | 大和抚子 | 与「温厚」「気品」有重叠，缺的是**敬语 + 献身 + 羞耻的拧巴**。 |
| **认知差** | 天然呆、小妹妹 | 「元気っ子」偏吵；**天然**偏慢半拍、自爆；**妹系**偏依赖称呼与撒娇语法。 |
| **腹黑 / 算计** | 毒舌笑面、小恶魔 | 「傲慢」不够「**笑着把你卖了**」。 |
| **丧 / 虚无** | 阴角、厌世 | 与无口不同：无口可寡言；**丧**是**否定句、自虐、兴趣稀薄**。 |
| **秩序恶 / 乐子人** | 混沌、挑事 | 强气偏对抗；**乐子人**偏**围观自己受害**或**挑衅主人**。 |
| **成熟色气** | 妖艳、痴女系姐姐 | 「姉御」偏强；缺**主动勾引、慢节奏荤话**。 |
| **常识人吐槽** | 正常人被迫害 | 适合喜剧调剂；与「生真面目」不同：**吐槽役 + 被迫参与**。 |

写口上时可自检：**陷落前 / 屈服 / 恋慕 / 隶属 / 崩坏** 是否各有一套语气；**处女・恋慕・害羞** 等素质是否做 `GROUPMATCH` 微分。

---

## 五、推荐 **8～10 个新「性格」**（供写入 `Str.csv` 3012～）

下列是**中文性格标签**建议（与现有 3000～3011 不重复即可）。口上套装名建议用 **`中文或日文+数字1`**，与 `KOJO_GENERAL_INDEX_*` 一致。

| # | 性格（CSTR） | 口上套装名（示例） | 一句话定位 |
|---|----------------|---------------------|--------------|
| 1 | **懦弱** | `懦弱1` | 未陷落：恳求、退缩、少量道歉；陷落：**顺从、示弱、不知所措**为主，多句近义随机；避免句句「对不起」。 |
| 2 | **大和抚子** | `大和抚子1` | 敬语、关心主人、羞耻时仍先顾礼节；陷落后可「献身」。 |
| 3 | **天然呆** | `天然1` | 慢半拍、会错意、自爆；H 台词**认真搞笑**。 |
| 4 | **妹妹系** | `妹系1` | 称呼黏、撒娇、依赖；反抗期→撒娇期的落差。 |
| 5 | **小恶魔** | `小恶魔1` | 笑面挑事、捉弄主人；**S 味轻**、以语言为主。 |
| 6 | **阴角／丧系** | `阴角1` | 低能量、否定句、兴趣薄；绝顶时**羞耻破防**反差。 |
| 7 | **痴女姐** | `妖艳1` | 慢节奏、主动勾引、荤话稳；与强气「拒否」相反。 |
| 8 | **常识人** | `吐槽役1` | 先吐槽再妥协；适合**地文喜剧**衔接。 |
| 9 |（可选）**女仆** | `女仆1` | 规矩与侍奉用语；陷落后可把羞耻当成“服侍的一部分”。 |
| 10 |（可选）**虔诚** | `虔诚1` | 罪与罚、奉主如神；洗脑/隶属格外合拍。 |

**落地顺序建议**：先写 **`例文` + `调教命令`（执行）+ `绝顶` + `射精`** 四条主链 → 再补 **`日程/事件/战斗`** → 最后 **`崩坏/洗脑/口枷`**。

### 五附、扩展性格口上一览（供你复制 `懦弱口上1.ERB` 后逐套写作）

下列与上表及现有泛用**不重复**；`Str.csv` 建议占用 **6111～**（当前 `6110` 为懦弱1，`6197`/`6198` 为系统用，请避开）。复制母版后请**全局替换**表中「替换三件套」三列（函数名与文件名按你实际命名一致）。

| 建议 Str 显示名 | 建议 `CSTR:使用口上` | 替换三件套（母版 `懦弱1` →） | 一句话定位 |
|-----------------|----------------------|--------------------------------|------------|
| 大和抚子1 | `大和抚子1` | `KOJO_GENERAL_INDEX_懦弱1`→`…_大和抚子1`；`KOJO_GENERAL_懦弱1_TRAIN`→`…_大和抚子1_TRAIN`；`懦弱口上1.ERB`→`大和抚子口上1.ERB` | 敬语、献身、羞耻仍顾礼节；陷落＝「身は主に委ねます」。 |
| 天然1 | `天然1` | `…天然1` / `…天然1_TRAIN` / `天然口上1.ERB` | 慢半拍、会错意、认真搞笑系 H 台词。 |
| 妹系1 | `妹系1` | `…妹系1` / `…妹系1_TRAIN` / `妹系口上1.ERB` | 黏称呼、依赖、撒娇；反抗期→撒娇落差。 |
| 小恶魔1 | `小恶魔1` | `…小恶魔1` / `…小恶魔1_TRAIN` / `小恶魔口上1.ERB` | 笑面捉弄、语言挑事；轻 S、非强气硬顶。 |
| 阴角1 | `阴角1` | `…阴角1` / `…阴角1_TRAIN` / `阴角口上1.ERB` | 低能量、否定句、兴致薄；绝顶时羞耻破防反差。 |
| 妖艳1 | `妖艳1` | `…妖艳1` / `…妖艳1_TRAIN` / `妖艳口上1.ERB` | 慢节奏痴女姐、主动勾引；与姉御「强」不同。 |
| 吐槽役1 | `吐槽役1` | `…吐槽役1` / `…吐槽役1_TRAIN` / `吐槽役口上1.ERB` | 常识人先吐槽再被迫妥协；喜剧地文衔接。 |
| 纪律1 | `纪律1` | `…纪律1` / `…纪律1_TRAIN` / `纪律口上1.ERB` | 报告体、命令口癖；破防后规矩崩坏。 |
| 势利1 | `势利1` | `…势利1` / `…势利1_TRAIN` / `势利口上1.ERB` | 讨价还价、谈条件；陷落可转「只认你」。 |
| 病弱1 | `病弱1` | `…病弱1` / `…病弱1_TRAIN` / `病弱口上1.ERB` | 气短、易晕、被照顾欲；H 时体力不支与依赖。 |
| 学者1 | `学者1` | `…学者1` / `…学者1_TRAIN` / `学者口上1.ERB` | 冷静分析身体反应；陷落后理性崩坏。 |
| 野孩子1 | `野孩子1` | `…野孩子1` / `…野孩子1_TRAIN` / `野孩子口上1.ERB` | 粗线条、直球害羞；与元气的「吵」不同偏野。 |
| 人妻1 | `人妻1` | `…人妻1` / `…人妻1_TRAIN` / `人妻口上1.ERB` | 包容口吻、「仕方ない」系；背德自觉。 |
| 反派颜1 | `反派颜1` | `…反派颜1` / `…反派颜1_TRAIN` / `反派颜口上1.ERB` | 嘴硬威胁弱；被压后嘴软身软。 |
| 虔诚1 | `虔诚1` | `…虔诚1` / `…虔诚1_TRAIN` / `虔诚口上1.ERB` | 罪与罚、奉主如神；洗脑/隶属格外合拍。 |
| 女仆1 | `女仆1` | `…女仆1` / `…女仆1_TRAIN` / `女仆口上1.ERB` | 侍奉口癖、礼仪用语、纪律化羞耻；崩坏时礼仪碎裂更刺眼。 |
| 愚忠1 | `愚忠1` | `…愚忠1` / `…愚忠1_TRAIN` / `愚忠口上1.ERB` | 不问对错只求服从；“献身＝证明忠诚”。可与虔诚区分为“人臣忠”而非“宗教忠”。 |
| 仇恨1 | `仇恨1` | `…仇恨1` / `…仇恨1_TRAIN` / `仇恨口上1.ERB` | 咬牙切齿、怨毒台词；陷落可走两路：①越爽越恨的扭曲，②恨意被快感磨碎。 |
| 知心1 | `知心1` | `…知心1` / `…知心1_TRAIN` / `知心口上1.ERB` | 温柔善解人意、助手属性明显；陷落后主动引导你更舒服，一个眼神就懂你想要什么。 |
| 拜金1 | `拜金1` | 可与势利合并或拆细：纯金钱动机 vs 势利嘴炮 | 明码标价、按次计费梗。 |
| 电波1 | `电波1` | `…电波1` / `…电波1_TRAIN` / `电波口上1.ERB` | 跳跃比喻、自说自话；与天然「慢」不同偏跳。 |
| 傲娇1 | `傲娇1` | `…傲娇1` / `…傲娇1_TRAIN` / `傲娇口上1.ERB` | 否认喜欢身体诚实；与强气「主导」区分。 |
| 仆从1 | `仆从1` | `…仆从1` / `…仆从1_TRAIN` / `仆从口上1.ERB` | 主仆称谓固定、卑微敬语；非懦弱「怕」偏身份。 |
| 复仇1 | `复仇1` | `…复仇1` / `…复仇1_TRAIN` / `复仇口上1.ERB` | 忍辱负重台词；陷落＝心防瓦解或假意顺从（二选一人设）。 |

**工程同步提醒**：新玩法命令（如 **足交 / 肛塞 / 尿道塞**）已在多数 `汎用口上/*_調教コマンド.ERB` 补 `CASE`；自写新性格时记得对照 **`docs/口上与玩法扩展同步规则.md`** 扫一遍缺项。

---

## 六、与 `性格` 随机池的衔接（可选）

随机性格在 `ERB/○キャラメイク/各種ランダム処理/ランダム_性格.ERB`：`STR_REF = 3000`，`STR_MAX` 需覆盖你在 **`Str.csv` 新增的性格行号范围**。  
新增性格后记得同步 **`ランダム_素質_性格・身分補正.ERB`** 等里若有 `SELECTCASE CSTR:性格` 的列表，否则随机到**新性格无补正**（不一定报错，但少风味）。

---

## 七、自检清单（交稿前）

- [ ] `@KOJO_GENERAL_INDEX_口上識別名` 与 `Str.csv` 6100 段、游戏内 `CSTR:使用口上` **三者同名**  
- [ ] `CASE "例文"` 有至少一条 `MESSAGE_BOX`  
- [ ] `调教命令` + `执行` 有通用品或按 `ARGS:1` 分指令  
- [ ] `绝顶` / `射精` 有基础分支（可按 `SOURCE:` 细分）  
- [ ] 长句用 `\n` 换行；**不要**在 `MESSAGE_BOX` 里混未闭合引号  
- [ ] 文件头写清作者与许可证（便于以后上仓库）

---

*文档版本：随仓库魔改自用；若你定稿新口上命名规则，可把「口上識別名」一节改成团队统一表。*
